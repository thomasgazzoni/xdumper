# xdumper configuration
# Copy this file to .env and fill in your values

# =============================================================================
# Backend Selection
# =============================================================================

# Backend to use: "twscrape" (default) or "patchright"
# - twscrape: Uses twscrape library with cookie-based authentication
# - patchright: Uses browser automation with bot detection evasion
XDUMPER_BACKEND=twscrape

# =============================================================================
# Patchright Backend Settings
# =============================================================================

# Path to Chrome user data directory for persistent browser sessions
# The browser profile stores cookies, localStorage, etc. for session persistence
# (default: ~/.xdumper/chrome-profile)
XDUMPER_CHROME_PROFILE=

# Run browser in headless mode (default: false)
# Set to "true" for headless operation, "false" to show browser window
# Note: headless=false is recommended for better stealth
XDUMPER_HEADLESS=false

# =============================================================================
# Twscrape Backend Settings
# =============================================================================

# Path to twscrape accounts database (default: ~/.xdumper/accounts.db)
# Stores Twitter account credentials and cookies for twscrape
XDUMPER_DB=

# =============================================================================
# Storage Settings
# =============================================================================

# Path to tweet storage database (default: ~/.xdumper/tweets.db)
# SQLite database for caching scraped tweets
XDUMPER_STORE=

# =============================================================================
# Network Settings
# =============================================================================

# Proxy URL for requests (optional, works with both backends)
# Examples:
#   socks5://127.0.0.1:1080
#   http://127.0.0.1:8080
#   http://user:pass@proxy.example.com:8080
XDUMPER_PROXY=

# =============================================================================
# Logging
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR (default: WARNING)
# Set to DEBUG to see detailed Patchright scraping logs
XDUMPER_LOG_LEVEL=WARNING
